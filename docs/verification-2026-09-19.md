# 全面改訂の検証記録

実施日：2026-09-19。対象：静的サイトと、隣接知識基盤の現行資料。ユーザーから最終精査とpushの依頼を受けた。本番への反映は、Gitへのpush成功とは区別して確認する。

## 最終精査（push対象）

- 料金の共通式、初期費用、追加開発費、0円・据置・減額、初期費用と月額前払いの区別を確認。
- 公開Googleフォームを読み取り確認。一次返信目安がフォーム3営業日・LP2営業日だったため、LPの規約・プライバシーを通常3営業日以内へ統一。外部フォームは送信・編集していない。
- 小規模事業所ページの「月額0円なら初期費用0円」を「導入時の月額」と明確化。
- 全12ページ×320・390・768・1440pxの48表示で横はみ出し・画像切れなし。スマホメニューの開閉・Escでのフォーカス復帰・リンク選択で閉じる動作を再確認。
- 実ブラウザーで月5時間・8時間・0分・1分・不正な60分入力とプリセット復帰を確認。
- 共通HTML生成・ローカル参照362件・計算21＋6ケースを再検証。一次返信目安の一致も自動テストへ追加。
- 非公開知識基盤・顧客情報をGit対象へ追加しない。代表的な秘密鍵・トークン形式のスキャンで検出なし。網羅的な秘密情報監査ではない。
- 対象リモートは `origin`（`otakekeke/landing-page`）、対象ブランチは `new-plan`。force push・mainへのマージ・外部フォームの変更は行わない。
- Gitのpush事前確認は成功。GitHub管理APIは認証できず、Pagesの自動公開設定は未確認。push後のリモート一致は別途確認する。
- フォーム内のプライバシー方針リンク・個人情報非送信の注意書き、個別契約の精算・責任・適用時期は[公開前チェック](release-checklist.md)に残す。これらを技術テスト済み・専門家確認済みとはしない。

以下は、コミット・push前の各改訂段階の記録。各段階の「未実施」はその時点の状況を示す。

## 同日追補：追加開発費＝月額の増加分×3か月分

標準範囲の機能追加費用を、月額の増加分の3か月分（税込・その追加につき一度のみ）へ変更。月額全体の3か月分・前払い・初期費用の再請求ではないこと、範囲外の事前見積りを明記した。月額30,000円→48,000円なら追加開発費54,000円。

- 生成HTML一致、全13HTMLのローカル参照362件、月額・初期費用21ケース、追加開発費6ケース（増額・据置・減額・0円から・1分分・両方0円）はPASS。
- トップと更新した下層8ページを幅320px・1440pxで確認。新料金表記を確認し、ページ全体の横はみ出しなし。トップ料金説明を両幅で目視確認し、追加FAQの展開と54,000円の説明を確認。
- ブラウザーの警告・エラーログなし。一時的な画面幅指定は解除。
- 知識基盤13文書のリンク55件を確認。Git差分の空白エラーなし。
- 新規導入シミュレーターは変更せず、追加開発費の試算は本文の計算例で説明。
- 作業前バックアップ：ローカル一時フォルダ `takenoko-extension-policy-20260919-081759`。LP編集元・規約・料金資料・現行事業資料を保存。既存契約・提出原本・実績は変更していない。
- 公開、コミット、外部フォーム・掲載先の変更は未実施。

以下は同日の先行改訂時の検証記録。

## 同日追補：初期費用3か月分・追加機能時の再計算

初期費用を導入時に合意した月額の3か月分へ変更し、シミュレーターに月額と初期費用を併記した。要望による追加機能・対象業務変更時の月額再計算と着手前合意を、LP・規約・関連資料へ反映した。

- 自動計算テストは21ケースへ拡張。各ケースで月額と初期費用の両方を確認し、PASS。8時間＝月額48,000円・新規導入初期費用144,000円も含む。
- 実ブラウザーで5時間＝30,000円／90,000円、8時間＝48,000円／144,000円、0分＝0円／0円、1分＝100円／300円、60分入力＝両方エラーを確認。
- 全12ページを320px・1440pxで再検査し横はみ出しなし。390pxで新しい初期費用欄を目視確認。320pxで最大入力の月額60,005,900円・初期費用180,017,700円もはみ出しなし。
- 生成HTMLの一致、ローカル参照362件、知識基盤13文書のリンク55件を再確認。
- シミュレーターの初期費用は新規導入時の試算。機能追加時の初期費用再請求や、追加開発費の計算機ではない。
- 作業前バックアップ：ローカル一時フォルダ `takenoko-initial-fee-20260919-080008`。既存契約・原本・実績は変更していない。

以下は全面刷新時の検証記録。計算テスト件数は上記の追補が最新。

## 自動検証

| 確認 | 結果 |
| --- | --- |
| ビルド後の生成元とHTMLの一致 | PASS |
| 全13HTML（12ページ＋旧参考URL）のローカル参照・アンカー362件 | PASS |
| 共通ナビ・フッター、H1、重複ID、旧月額表示、不要な計測タグ等 | PASS |
| 料金計算20ケース（0分、1分、時間＋分、空欄、負数、範囲外、小数、プリセット） | PASS |
| JavaScript・生成元・任意フォーム補助スクリプトの構文 | PASS（フォーム補助は実行せず構文確認のみ） |
| Git差分の空白エラー | なし（既存のCRLFを行末として判定） |
| 知識基盤の改訂Markdown13文書・ローカルリンク55件 | PASS |

Web検証は `npm run check:generated` と `npm test` で再実行できる。資料確認は今回の作業用スクリプトで、対象文書の文字化け、改訂日、参照先を確認した。知識基盤をWebリポジトリに取り込んだものではない。

## ブラウザー検証

Chromeのローカルプレビューで実施。

- 12ページすべてを幅320px・390px・768px・1440pxで検査。ページ全体の横はみ出しなし。
- PC・390pxで画像の読込みを検査し、画像切れなし。
- トップ、管理者ページ、法務ページ、料金表の代表レイアウトを画像で目視確認。
- モバイルメニューの開閉、Escで閉じてボタンへフォーカスを戻す動作、下層ページからトップ料金欄への遷移を確認。
- 実ブラウザーでも0分＝0円、1分＝100円、60分入力のエラー表示を確認。
- 規約の横長料金表はページ幅内でスクロール。フォーカスと右矢印キーでの横移動を確認。
- `index.dc.html` から現行トップへ転送されることを確認。
- 確認終了時のブラウザーログに警告・エラーなし。画面幅の一時上書きは解除。

全体ページ画像の取得は一度タイムアウトしたため、通常画面のキャプチャとスクロール・DOMによる確認を使用した。新しいLighthouseスコアは取得していない。

## 原本と対象外

投稿・応募Excel2点はSpreadsheetsスキルで読み取り確認した。旧月額の記載はなく、提出時の記録を保持。CARISOの現行文案は別添として作成し、外部に再投稿していない。

検証は実サービスの効果、補助金対象性、法的妥当性、Googleフォームの送信結果を保証しない。外部フォームの送信・編集、契約変更、デプロイ、コミットは行っていない。[公開前チェック](release-checklist.md)を別途確認する。
